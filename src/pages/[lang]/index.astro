---
import Welcome from "../../components/Welcome.astro";
import Layout from "../../layouts/Layout.astro";
export function getStaticPaths() {
  return ["en", "de", "ru"].map((lang) => ({ params: { lang } }));
}
---

<Layout>
  <Welcome />
</Layout>
